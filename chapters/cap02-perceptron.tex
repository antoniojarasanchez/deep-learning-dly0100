% ==================================================
%  CAPÍTULO 2: EL PERCEPTRÓN Y REDES FULLY CONNECTED
% ==================================================
\cleardoublepage
\chapter{El Perceptrón y Redes Fully Connected Feed Forward}

\section{De la IA a las redes neuronales}

Una red se puede pensar como una malla de nodos entrelazados con diferentes uniones. En Deep Learning programamos un sistema que en cada capa decide hacia qué nodo avanzar según una probabilidad dada por un algoritmo.

En \textbf{\emph{Representation Learning}} (aprendizaje de representaciones) se construyen representaciones de las entradas o inputs en etapas sucesivas. En cada etapa, la representación o abstracción de la anterior contiene información más compacta y relevante, pero en menor cantidad. Eventualmente, con esta abstracción sucesiva se crea un tensor.Porque transforman la entrada inicial hasta generar un tensor o vector final que contiene todo lo necesario para tomar una decisión, estos algoritmos son llamados \textbf{\emph{End-to-End}}.

La diferencia fundamental entre Machine Learning y Deep Learning radica en la \emph{extracción de características}:

\begin{table}[H]
\centering
\caption{Extracción de características: ML vs DL.}
\label{tab:representacion}
\small
\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Enfoque} & \textbf{Proceso} \\
\midrule
Machine Learning & Extracción manual de características $\to$ clasificador $\to$ salida \\
Deep Learning & Entrada $\to$ red neuronal $\to$ salida, sin intervención manual \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Rendimiento, datos y costo computacional:}
A diferencia de los modelos tradicionales de Machine Learning (cuyo rendimiento se satura rápidamente al aumentar los datos), los modelos de Deep Learning escalan a medida que se incrementan la cantidad de datos, el número de capas y la capacidad computacional.
La idea es que se \emph{podría} resolver prácticamente cualquier problema, dados una cantidad de datos y capacidad de cómputo suficientes, siendo estos mismos el factor limitante.

\section{El perceptrón}

El \textbf{perceptrón} se inspira en el funcionamiento de la red neuronal biológica del cerebro humano. En este, cada neurona recibe impulsos eléctricos de neuronas vecinas. Cuando la cantidad total de las señales entrantes supera cierto \textbf{umbral}, la neurona ``dispara'' su propia señal, la cual se conecta con otras neuronas.

La unión o punto de contacto entre dos neuronas se llama \textbf{sinapsis}. Cuando aprendemos nuevas asociaciones entre dos conceptos, la \textbf{fuerza sináptica} entre las neuronas que representan dichos conceptos se fortalece.

\textbf{Regla de Hebb (1949)}: ``Las células que se \textbf{activan juntas}, se conectan entre sí''.

\begin{definicion}
\textbf{Perceptrón (Rosenblatt, 1957)}: modelo simplificado de una neurona biológica llevado
al computador.
\end{definicion}

\subsection{Operación del perceptrón}

\paragraph{Intuición}
El perceptrón es la unidad básica de una red neuronal. Recibe un conjunto de $m$ señales de entradas binarias $x_i$ (por ejemplo, píxeles encendidos o apagados), cada una con un cierto nivel de \textbf{importancia} (los pesos $w_i$), y un \textbf{umbral de activación} ajustable (el sesgo o bias $b$). 
El perceptrón suma todas las señales \emph{ponderadas} y, si el total supera cierto umbral, se ``activa'' y devuelve 1; en caso contrario, devuelve 0. 
Podría decirse que este proceso de decisión binaria lo convierte en un clasificador.
\emph{El diagrama de la Figura~\ref{fig:perceptron} ilustra estos componentes, los cuales se definen formalmente a continuación.}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  inpt/.style={draw,rectangle,rounded corners,minimum width=16mm,minimum height=7mm,fill=dlblueLight},
  wgt/.style={draw,rectangle,minimum width=14mm,minimum height=7mm,fill=white},  % Cajas para los pesos
  op/.style={draw,circle,minimum size=9mm,inner sep=0pt,fill=dlgreenLight},
  lbl/.style={font=\small},
]
  % Entradas (x_i)
  \node[inpt] (x1) at (0,2.5) {$x_1$};
  \node[inpt] (x2) at (0,0) {$x_2$};
  \node at (0,-1.25) {$\vdots$};  % Puntos verticales
  \node[inpt] (xm) at (0,-2.5) {$x_m$};

  % Pesos como cajas (w_i)
  \node[wgt] (w1) at (2,2.5) {$w_1$};
  \node[wgt] (w2) at (2,0) {$w_2$};
  \node[wgt] (wm) at (2,-2.5) {$w_m$};

  % Sesgo (b) con flecha directa a la suma
  \node[draw,rectangle,rounded corners,minimum width=12mm,minimum height=7mm,fill=dlorangeLight] (bias) at (3.8,2.0) {$b$};

  % Nodos principales: suma ponderada, activación y salida
  \node[op] (sum) at (3.8,0) {$\Sigma$};
  \node[op] (act) at (6.2,0) {$\phi$};
  \node[inpt,fill=dlorangeLight] (out) at (8.4,0) {$\hat{y}$};

  % Flechas desde x_i hacia w_i
  \draw[-{Stealth}] (x1) -- (w1);
  \draw[-{Stealth}] (x2) -- (w2);
  \draw[-{Stealth}] (xm) -- (wm);

  % Flechas desde w_i hacia la suma ponderada
  \draw[-{Stealth}] (w1) -- (sum);
  \draw[-{Stealth}] (w2) -- (sum);
  \draw[-{Stealth}] (wm) -- (sum);

  % Flecha del sesgo a la suma (sin peso intermedio)
  \draw[-{Stealth}] (bias) -- node[midway,right,lbl]{sesgo} (sum);

  % Flechas internas: suma -> activación -> salida
  \draw[-{Stealth}] (sum) -- node[midway,above,lbl]{$u$} (act);
  \draw[-{Stealth}] (act) -- node[midway,above,lbl]{$\phi(u)$} (out);

% Distinción final: flechas directas de \hat{y} hacia las cajas 1 y 0
  \node[draw,rectangle,minimum width=10mm,minimum height=6mm] (out1) at (10.2,0.7) {$1$};
  \node[draw,rectangle,minimum width=10mm,minimum height=6mm] (out0) at (10.2,-0.7) {$0$};
  \draw[-{Stealth}] (out.east) -- (out1.west);
  \draw[-{Stealth}] (out.east) -- (out0.west);

  % Etiquetas explicativas
  \node[lbl] at (3.7,-1.0) {suma ponderada};
  \node[lbl] at (6.3,-1.0) {activación};
  \node[font=\small] at (0,-3.6) {entradas $x_i$};
  \node[font=\small] at (2,-3.6) {pesos $w_i$};
  \node[lbl] at (9,-1.7) {salida binaria};
\end{tikzpicture}
\caption{Diagrama del perceptrón: suma ponderada de las entradas y función de activación.}
\label{fig:perceptron}
\end{figure}

\paragraph{Definición formal}
Más rigurosamente, entendemos al perceptrón como una función compuesta por una función de combinación lineal o suma ponderada ($u(\vec{x}, \vec{w}, b)$) y una función de activación escalón ($\phi(u)$).

El perceptrón recibe tres argumentos: un vector columna de entradas binarias $\vec{x}$, un vector columna de pesos $\vec{w}$ y un número real o escalar $b$, llamado sesgo. La función, tras procesar las entradas, devuelve un valor binario $\hat{y}$, que indica si este fue activado o no. Se detallan a continuación:

\begin{itemize}[topsep=2pt,itemsep=1pt]
  \item $\vec{x} = (x_1, x_2, \ldots, x_m)^\top \in \{0,1\}^m$ es el vector columna de entradas binarias (características). Los valores $x_i$ solo pueden ser $0$ o $1$, y $m$ es la cantidad de dimensiones del vector, es decir, la cantidad de entradas.
  \item $\vec{w} = (w_1, w_2, \ldots, w_m)^\top \in \mathbb{R}^m$ es el vector columna de \textbf{pesos} (\emph{weights}), que ponderan o escalan la importancia de cada entrada binaria. Los valores $w_i$ son números reales, y la dimensión $m$ coincide con la de $\vec{x}$, condición necesaria para que el producto escalar $\vec{w}^\top \vec{x}$ esté definido.
  \item $b \in \mathbb{R}$ es el \textbf{sesgo} (\emph{bias}), un parámetro de desplazamiento u \emph{offset} que ajusta el umbral de activación de la neurona.
  \item $\hat{y} \in \{0,1\}$ es el resultado binario final de la función, que indica la activación (o no) del perceptrón.
\end{itemize}

Así, queda definido el dominio de la función del perceptrón, y su proceso se detalla a continuación:

$$P: \{0,1\}^m \times \mathbb{R}^m \times \mathbb{R} \to \{0,1\}$$

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  scale=1.2,
  transform shape,
  node distance=0.8cm and 1.2cm,
  box/.style={draw, rectangle, rounded corners, minimum width=2.2cm, minimum height=0.9cm, align=center, font=\scriptsize},
  arrow/.style={-{Stealth}, thick},
  label/.style={font=\tiny, align=center}
]
  \node[box, fill=dlblueLight!30] (input) {
    $\vec{x} \in \{0,1\}^m$ \\
    $\vec{w} \in \mathbb{R}^m$ \\
    $b \in \mathbb{R}$
  };
  \node[box, fill=dlgreenLight!30, right=of input] (linear) {
    $u = \vec{w}^\top \vec{x} + b$ \\
    $\in \mathbb{R}$
  };
  \node[box, fill=dlorangeLight!30, right=of linear] (activation) {
    $\phi(u) = \begin{cases}1 & u \ge 0\\0 & u<0\end{cases}$ \\
    $\in \{0,1\}$
  };
  \node[box, fill=red!20, right=of activation] (output) {
    $\hat{y} = \phi(u)$ \\
    $\in \{0,1\}$
  };

  \draw[arrow] (input) -- node[label, above] {producto punto \\ + sesgo} (linear);
  \draw[arrow] (linear) -- node[label, above] {escalón} (activation);
  \draw[arrow] (activation) -- node[label, above] {composición} (output);

  % Etiquetas inferiores (más compactas)
  \node[font=\tiny, below=0.1cm of input] {Vector / escalar};
  \node[font=\tiny, below=0.1cm of linear] {Escalar real};
  \node[font=\tiny, below=0.1cm of activation] {Binario};
  \node[font=\tiny, below=0.1cm of output] {Binario final};
\end{tikzpicture}
\caption{Recorrido de las variables en el perceptrón.}
\label{fig:viajevariables}
\end{figure}

\paragraph{Paso 1: Entrada neta}
Primero, se calcula la suma \emph{ponderada} de las entradas más el sesgo. Esta operación pondera los vectores $\vec{x}$ y $\vec{w}$, cuyo producto escalar las combina en un número real, y le suma el sesgo $b$. Formalmente:

$$u: \{0,1\}^m \times \mathbb{R}^m \times \mathbb{R} \to \mathbb{R}$$

\begin{equation}
\label{eq:perceptron-u}
u(\vec{x}, \vec{w}, b) = \vec{w}^\top \vec{x} + b = \sum_{i=1}^{m} w_i x_i + b = w_1 x_1 + w_2 x_2 + \cdots + w_m x_m + b
\end{equation}

\textbf{Nota sobre la notación:} Dado que los vectores $\vec{x}$ y $\vec{w}$ se definen como vectores columna, la transpuesta $\vec{w}^\top$ convierte a $\vec{w}$ en un vector fila para que el producto $\vec{w}^\top \vec{x}$ (fila por columna) sea un escalar, que corresponde a la suma $\sum_{i=1}^{m} w_i x_i$.

\textbf{Nota sobre variantes del diagrama:} El diagrama muestra la representación \textbf{directa} del sesgo, donde $b$ se suma como término independiente: $u = \sum_{i=1}^{m} w_i x_i + b$. Existe una variante equivalente, que añade una entrada ficticia $x_0 = 1$ y un peso $w_0 = b$, unificando la operación en una única suma ponderada:
\[
u = \sum_{i=0}^{m} w_i x_i = w_0 \cdot 1 + w_1 x_1 + \cdots + w_m x_m = b + w_1 x_1 + \cdots + w_m x_m.
\]
Su utilidad es principalmente \textbf{algebraica}: permite expresar el perceptrón como un único producto punto $\vec{w}^\top \vec{x}$, simplificando derivadas, reglas de aprendizaje y demostraciones teóricas. Ambas representaciones son matemáticamente equivalentes y su elección es contextual.

\paragraph{Paso 2: Función de activación}
El valor resultante $u$ (que es un número real) se transforma en una salida binaria, que es $1$ si $u \geq 0$, y $0$ si $u < 0$. Esto se logra mediante la función de activación escalón unitario, la cual introduce la no linealidad y toma la decisión final de activación:

$$\phi: \mathbb{R} \to \{0,1\}$$

\begin{equation}
\phi(u) = 
\begin{cases} 
1 & \text{si } u \geq 0, \\
0 & \text{si } u < 0.
\end{cases}
\end{equation}

La Figura~\ref{fig:escalon} muestra su forma: para cualquier valor negativo de $u$, la salida es $0$; para $u$ igual o mayor que cero, la salida es $1$.

\begin{figure}[H]
\centering
\begin{tikzpicture}[scale=0.8]
  % Ejes
  \draw[-{Stealth}] (-3.5,0) -- (3.5,0) node[right] {$u$};
  \draw[-{Stealth}] (0,-0.5) -- (0,2.5) node[above] {$\phi(u)$};
  % Línea horizontal en y=0 para u<0
  \draw[thick] (-3,0) -- (0,0);
  % Línea horizontal en y=1 para u>0
  \draw[thick] (0,1) -- (3,1);
  % Punto abierto en (0,0) (no incluido)
  \filldraw[fill=white,draw=black] (0,0) circle (2pt);
  % Punto cerrado en (0,1) (incluido)
  \filldraw[fill=black,draw=black] (0,1) circle (2pt);
  % Etiquetas de los ejes
  \node at (-0.3,-0.3) {$0$};
  \node at (-0.3,1.1) {$1$};
\end{tikzpicture}
\caption{Función de activación escalón unitario.}
\label{fig:escalon}
\end{figure}

  Notar que más adelante, la función escalón se vuelve obsoleta, y eventualmente es reemplazada por la función ReLU.

\paragraph{Paso 3: Salida final del perceptrón}
Finalmente, la salida del modelo es la composición de la combinación lineal afín (suma ponderada) y la función de activación:
\begin{equation}
\label{eq:perceptron-y}
\hat{y} = P(\vec{x}, \vec{w}, b) = \phi\bigl( u(\vec{x}, \vec{w}, b) \bigr) 
= \phi\bigl( \vec{w}^\top \vec{x} + b \bigr).
\end{equation}

En resumen, el perceptrón transforma un vector de entradas binarias $\vec{x}$ en una salida binaria $\hat{y}$ mediante una suma ponderada ajustada por un sesgo y una decisión de umbral. Los parámetros del modelo, $\vec{w}$ y $b$, son los que se ajustan durante el entrenamiento para que la salida $\hat{y}$ se aproxime al valor deseado para cada ejemplo de entrada.

\subsection{Funciones de activación}

Una función de activación es una función no lineal que transforma la entrada neta $u$ de una neurona en una señal de salida. Biológicamente, intenta emular el comportamiento de una neurona real: cuando la acumulación de estimulación supera un cierto umbral, la neurona ``dispara'' información hacia la siguiente neurona.

En las funciones de activación \emph{siempre} se deben utilizar funciones \textbf{no lineales}. Si se usaran funciones lineales, la composición de múltiples capas sería equivalente a una única transformación lineal, anulando la capacidad de la red para aprender patrones complejos.

A continuación se estudian las cuatro funciones de activación más relevantes: la función escalón (histórica), la sigmoide, la tangente hiperbólica (tanh) y ReLU.

Originalmente, Rosenblatt utilizó la función escalón en su perceptrón:

\begin{definicion}
Función escalón (step)
\begin{equation}
\label{eq:step}
\phi(u) = 
\begin{cases}
0 & \text{si } u < 0, \\
1 & \text{si } u \geq 0.
\end{cases}
\end{equation}
\end{definicion}
Esta función \emph{no es utilizable} en el entrenamiento moderno. Para entrenar una red neuronal necesitamos calcular derivadas (gradientes) mediante retropropagación, y la derivada de esta función es $0$ en todos los puntos donde está definida (es una función constante por tramos). Esto impide que los pesos se actualicen.

La función que resolvió el problema de la derivabilidad es la \textbf{sigmoide logística}
\begin{definicion}
Función sigmoide
\begin{equation}
\label{eq:sigmoide}
\sigma(u) = \frac{1}{1 + e^{-u}}.
\end{equation}
Es derivable en todo $\mathbb{R}$ y su rango es $(0, 1)$, lo que la hace adecuada para interpretar salidas como probabilidades. Emula razonablemente bien el comportamiento de una neurona biológica.
\end{definicion}
Su principal desventaja es la \textbf{saturación en los extremos}: para valores de $u$ muy grandes o muy pequeños, la pendiente (gradiente) es prácticamente $0$, lo que provoca que los gradientes desaparezcan durante la retropropagación y la red deje de aprender.

\begin{definicion}
Función tangente hiperbólica (tanh)
\begin{equation}
\label{eq:tanh}
\tanh(u) = \frac{e^{u} - e^{-u}}{e^{u} + e^{-u}}.
\end{equation}
Su rango es $(-1, 1)$, por lo que está centrada en cero, lo que facilita el entrenamiento. Funciona mejor que la sigmoide en la práctica.
\end{definicion}
Presenta el mismo problema de saturación en los extremos: el gradiente se vuelve muy pequeño para valores grandes de $|u|$, lo que puede ralentizar o detener el aprendizaje.

\begin{definicion}
Función ReLU (Rectified Linear Unit)
\begin{equation}
\label{eq:relu}
\text{ReLU}(u) = 
\begin{cases}
0 & \text{si } u < 0, \\
u & \text{si } u \geq 0.
\end{cases}
\end{equation}
Es la función de activación más utilizada en la práctica actual. Para valores positivos, el gradiente es $1$ y \emph{nunca desaparece}, lo que acelera el entrenamiento significativamente.
\end{definicion}

\textbf{Regla de uso}: ReLU se utiliza en las \emph{capas ocultas} (intermedias). \emph{No se usa en la capa de salida} para clasificación; en su lugar se emplea softmax. Tampoco se recomienda usar ReLU cuando los datos de entrada pueden tomar valores negativos muy grandes, ya que las neuronas pueden ``morir'' (quedar atascadas en $0$).

\begin{table}[H]
\centering
\caption{Comparación de las funciones de activación.}
\label{tab:activaciones}
\small
\begin{tabularx}{\textwidth}{@{}lXXX@{}}
\toprule
\textbf{Función} & \textbf{Rango} & \textbf{Ventajas} & \textbf{Desventajas} \\
\midrule
Escalón & $\{0,1\}$ & Histórica, simple & No derivable, gradiente cero \\
Sigmoide & $(0,1)$ & Derivable, interpretable & Saturación en extremos \\
Tanh & $(-1,1)$ & Centrada en cero & Saturación en extremos \\
ReLU & $[0,\infty)$ & No satura para positivos, rápida & Neuronas ``muertas'' para negativos \\
\bottomrule
\end{tabularx}
\end{table}

\section{Arquitectura de una red neuronal}

\subsection{Multi-perceptrón}

\paragraph{Limitación del perceptrón simple:}

El perceptrón simple (una sola neurona) solo puede aprender funciones linealmente separables, es decir, problemas donde los datos se pueden dividir por una línea recta. Para problemas más complejos, necesitamos redes con múltiples neuronas organizadas en capas: el \textbf{multi-perceptrón} o \emph{red neuronal multicapa}.

La realidad requiere extender el perceptrón a más dimensiones. En una red multicapa, el vector de entrada ya no se conecta a una única neurona, sino a \emph{muchas neuronas} en paralelo. Esto da lugar a una \textbf{matriz de pesos} (no un solo vector de pesos). Es decir, pasamos de $\vec{w} \in \mathbb{R}^m$ a $\vec{W}^{(i)} \in \mathbb{R}^{F_{i-1} \times F_{i}}$.

\paragraph{Componentes de una red neuronal multicapa}

\begin{enumerate}
  \item \textbf{Capa de entrada}: contiene los datos de entrada. No es una capa en el sentido estricto, ya que no realiza ninguna transformación; simplemente distribuye los datos a la siguiente capa.
  \item \textbf{Capas ocultas (escondidas)}: son las capas intermedias donde ocurren las transformaciones no lineales de los datos. La red aprende representaciones cada vez más abstractas a medida que avanzan las capas.
  \item \textbf{Capa de salida}: entrega el resultado final de la red. Para problemas de clasificación, suele ser un vector de probabilidades (usando softmax); para regresión, un escalar o vector de valores continuos.
\end{enumerate}

Es importante notar que al aumentar la cantidad de neuronas por capa y/o el número de capas de la red, aumentamos drásticamente la cantidad de \textbf{parámetros} (pesos y sesgos), lo que incrementa el tiempo de entrenamiento y la demanda de memoria.

\paragraph{Tipos de redes neuronales}

\begin{itemize}
\item \textbf{Red neuronal profunda (\textit{deep neural network})}: una red neuronal que tiene \emph{más de una capa oculta}. Cuantas más capas tiene, más profunda se considera.

\item \textbf{Fully Connected (\textit{completamente conectada})}: una arquitectura donde todas las neuronas de una capa están conectadas con todas las neuronas de la siguiente capa.

\item \textbf{Feed Forward (\textit{hacia adelante})}: la información fluye siempre en una dirección, desde la entrada hasta la salida, sin ciclos ni retroalimentación.
\end{itemize}

\subsection{Redes Feed Forward Fully Connected (FFFC)}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    scale=1, every node/.style={transform shape},
    layer_box/.style={draw=black!25, fill=gray!25, rounded corners=2pt},
    neuron/.style={circle, fill=gray!70, text=white, minimum size=7mm, inner sep=0pt, font=\footnotesize\bfseries},
    conn/.style={draw=blue!70!black, line width=0.7pt},
    bias_arrow/.style={-{Stealth[length=2mm, width=1.4mm]}, draw=blue!70!black, thick},
    bracket/.style={draw=blue!70!black, thick}
]

    % ================= Capa de Entrada (X) =================
    \node[layer_box, minimum width=1.1cm, minimum height=5.2cm] (boxX) at (0, 0) {};
    \node[neuron, fill=gray!60] at (0, 2.0) {};
    \node[neuron, fill=gray!60] at (0, 1.1) {};
    \node[neuron, fill=gray!60] at (0, 0.2) {};
    \node[font=\normalsize, text=gray!80] at (0, -0.9) {$\vdots$};
    \node[neuron, fill=gray!60] at (0, -2.0) {};
    \node[below=2mm of boxX, font=\small\bfseries] {$\vec{X}$};

    % Delimitador izquierdo F
    \draw[bracket] (-0.75, 2.6) -- (-0.9, 2.6) -- (-0.9, -2.6) -- (-0.75, -2.6);
    \node[left=10pt of boxX, font=\small\bfseries] {$F$};

    % ================= Capa Oculta 1 (h^1) =================
    \node[layer_box, minimum width=1.1cm, minimum height=4.3cm] (boxH1) at (2.8, 0) {};
    \node[neuron] at (2.8, 1.55) {$f^{(1)}$};
    \node[neuron] at (2.8, 0.7) {$f^{(1)}$};
    \node[neuron] at (2.8, -0.15) {$f^{(1)}$};
    \node[font=\normalsize, text=gray!80] at (2.8, -0.9) {$\vdots$};
    \node[neuron] at (2.8, -1.55) {$f^{(1)}$};
    \node[below=2mm of boxH1, font=\small\bfseries] {$\vec{h}^{(1)}$};
    \node[above=2mm of boxH1, font=\small\bfseries] {$f^{(1)}$};

    % Sesgo b1
    \draw[bias_arrow] (1.8, 2.8) node[above left, font=\small\bfseries] {$\vec{b}^{(1)}$} -- (boxH1.north west);

    % Conexiones X -> h^1 y Etiqueta W^1
    \draw[conn] (boxX.north east) -- (boxH1.north west);
    \draw[conn] (boxX.south east) -- (boxH1.south west);
    \node at (1.4, 0) {\large $\vec{W}^{(1)}$};

    % ================= Capa Oculta 2 (h^2) =================
    \node[layer_box, minimum width=1.1cm, minimum height=3.5cm] (boxH2) at (5.4, 0) {};
    \node[neuron] at (5.4, 1.2) {$f^{(2)}$};
    \node[neuron] at (5.4, 0.4) {$f^{(2)}$};
    \node[font=\normalsize, text=gray!80] at (5.4, -0.4) {$\vdots$};
    \node[neuron] at (5.4, -1.2) {$f^{(2)}$};
    \node[below=2mm of boxH2, font=\small\bfseries] {$\vec{h}^{(2)}$};
    \node[above=2mm of boxH2, font=\small\bfseries] {$f^{(2)}$};

    % Sesgo b2
    \draw[bias_arrow] (4.8, 2.5) node[above left, font=\small\bfseries] {$\vec{b}^{(2)}$} -- (boxH2.north west);

    % Conexiones h^1 -> h^2 y Etiqueta W^2
    \draw[conn] (boxH1.north east) -- (boxH2.north west);
    \draw[conn] (boxH1.south east) -- (boxH2.south west);
    \node at (4.1, 0) {\large $\vec{W}^{(2)}$};

    % ================= Separación ampliada: Puntos (3) y W^L centrada =================
    \node[font=\large, text=black!80] at (6.65, 0) {$\dots$};
    \node at (7.65, 0) {\large $\vec{W}^{(L)}$};

    % ================= Capa Oculta L (h^L) =================
    \node[layer_box, minimum width=1.1cm, minimum height=4.3cm] (boxHL) at (8.7, 0) {};
    \node[neuron] at (8.7, 1.55) {$f^{(L)}$};
    \node[neuron] at (8.7, 0.7) {$f^{(L)}$};
    \node[neuron] at (8.7, -0.15) {$f^{(L)}$};
    \node[font=\normalsize, text=gray!80] at (8.7, -0.9) {$\vdots$};
    \node[neuron] at (8.7, -1.55) {$f^{(L)}$};
    \node[below=2mm of boxHL, font=\small\bfseries] {$\vec{h}^{(L)}$};
    \node[above=2mm of boxHL, font=\small\bfseries] {$f^{(L)}$};

    % Sesgo bL
    \draw[bias_arrow] (7.8, 2.7) node[above left, font=\small\bfseries] {$\vec{b}^{(L)}$} -- (boxHL.north west);

    % Conexiones hacia h^L
    \draw[conn] (7.2, 1.5) -- (boxHL.north west);
    \draw[conn] (7.2, -1.5) -- (boxHL.south west);

    % Etiqueta L (número total de capas ocultas) debajo de los puntos
    \node at (6.65, -1.8) {\small $L$};

    % ================= Capa de Salida =================
    \node[layer_box, minimum width=1.1cm, minimum height=2.6cm] (boxOut) at (11.1, 0) {};
    \node[neuron] at (11.1, 0.75) {$g$};
    \node[font=\normalsize, text=gray!80] at (11.1, 0.0) {$\vdots$};
    \node[neuron] at (11.1, -0.75) {$g$};

    % Sesgo c
    \draw[bias_arrow] (11.0, 2.0) node[above left, font=\small\bfseries] {$\vec{c}$} -- (boxOut.north west);

    % Conexiones h^L -> Salida y Etiqueta U
    \draw[conn] (boxHL.north east) -- (boxOut.north west);
    \draw[conn] (boxHL.south east) -- (boxOut.south west);
    \node at (9.9, 0) {\large $\vec{U}$};

    % Delimitador derecho C y predicción \hat{y} con espaciado limpio
    \draw[bracket] (11.8, 1.3) -- (11.95, 1.3) -- (11.95, -1.3) -- (11.8, -1.3);
    \node[right=8pt of boxOut, font=\small\bfseries] {$C$};
    \node[font=\large] at (13.6, 0) {$\hat{\mathbf{y}}$};

\end{tikzpicture}
\caption{Arquitectura de una red Feed Forward.}
\label{fig:red_feedforward}
\end{figure}

\begin{multicols}{2}
\begin{itemize}
    \item[] \textbf{Capa de entrada}
    \item $\vec{X} \in \mathbb{R}^{n \times F}$: Matriz de entrada (datos), donde $n$ es el tamaño del lote (\emph{batch size}) y $F$ es el número de características de entrada. Para un solo ejemplo, $\vec{X} \in \mathbb{R}^{1 \times F}$.
    \item $F$: Número de neuronas (características) de la capa de entrada.
    
    \item[] \textbf{Capas ocultas ($i=1,\dots,L$)}
    \item $L$: Número total de capas ocultas.
    \item $\vec{W}^{(i)} \in \mathbb{R}^{F_{i-1} \times F_i}$: Matriz de pesos de la capa oculta $i$, donde $F_{i-1}$ es el número de neuronas de la capa anterior y $F_i$ el de la capa actual.
    \item $\vec{b}^{(i)} \in \mathbb{R}^{1 \times F_i}$: Vector fila de sesgos de la capa oculta $i$.
    \item $f^{(i)}: \mathbb{R} \to \mathbb{R}$: Función de activación (aplicada elemento a elemento) de la capa oculta $i$.
    \item $\vec{h}^{(i)} \in \mathbb{R}^{n \times F_i}$: Matriz de salida de la capa oculta $i$ (o vector fila $\mathbb{R}^{1 \times F_i}$ para un solo ejemplo).
    
    \item[] \textbf{Capa de salida}
    \item $\vec{U} \in \mathbb{R}^{F_L \times C}$: Matriz de pesos de la capa de salida.
    \item $\vec{c} \in \mathbb{R}^{1 \times C}$: Vector fila de sesgos de la capa de salida.
    \item $g: \mathbb{R} \to \mathbb{R}$: Función de salida (aplicada elemento a elemento), como softmax o identidad.
    \item $\hat{\mathbf{y}} \in \mathbb{R}^{n \times C}$: Matriz de salida final de la red (o vector fila $\mathbb{R}^{1 \times C}$ para un solo ejemplo), que contiene las predicciones.
    \item $C$: Número de neuronas de la capa de salida (número de clases para clasificación, o 1 para regresión).
\end{itemize}
\end{multicols}

\paragraph{Fórmula de la capa oculta}

Para cada capa oculta $i$, la salida de tal capa se calcula como:

\begin{equation}
\label{eq:capa-oculta}
\vec{h}^{(i)} = f^{(i)}\!\left( \vec{h}^{(i-1)} \, \vec{W}^{(i)} + \vec{b}^{(i)} \right)
\end{equation}

donde asumimos que $\vec{h}^{(0)} = \vec{X}$, es decir, la entrada es la capa $0$.

La multiplicación $\vec{h}^{(i-1)} \, \vec{W}^{(i)}$ es una operación matricial donde cada fila de $\vec{h}^{(i-1)}$ se multiplica por la matriz de pesos. El resultado se suma al vector de sesgos $\vec{b}^{(i)}$, y luego se aplica la función de activación $f^{(i)}$ elemento a elemento.

\textbf{Sobre la notación:} Acá los vectores se representan como \textbf{filas} y las matrices de pesos se multiplican por la derecha ($\vec{h} \, \vec{W}$). Esto difiere de la notación de vectores columna utilizada en el perceptrón simple ($\vec{w}^\top \vec{x}$), pero ambas son matemáticamente equivalentes.

\paragraph{Fórmula de la capa de salida}

La salida de la red se calcula como:

\begin{equation}
\label{eq:capa-salida}
\hat{\mathbf{y}} = g\!\left( \vec{h}^{(L)} \, \vec{U} + \vec{c} \right),
\end{equation}

donde $L$ es el número total de capas ocultas, y $\hat{\mathbf{y}}$ es la salida final de la red.


\paragraph{Aspectos técnicos de las operaciones matriciales}

En la práctica, para aprovechar el hardware moderno (GPUs, TPUs), los datos se procesan en \textbf{lotes} (\emph{batches}) en lugar de ejemplo por ejemplo. Esto significa que $\vec{X}$ ya no es un vector de tamaño $(F, 1)$, sino una \textbf{matriz de tamaño $(n, F)$}, donde $n$ es el tamaño del lote (\emph{batch size}) y $F$ es el número de características de entrada. De manera análoga, la salida $\hat{\mathbf{y}}$ es una matriz de tamaño $(n, C)$.

Las librerías modernas como TensorFlow y PyTorch están altamente optimizadas para operaciones matriciales en GPU, lo que acelera drásticamente los cálculos.

\subsection{Función de salida: Softmax}

Para problemas de clasificación multiclase, se utiliza la función \textbf{softmax} en la capa de salida.

\begin{definicion}
\textbf{Softmax}: función de salida que transforma un vector de valores reales en una \emph{distribución de probabilidades} sobre las clases. Para la componente $j$ del vector de salida:
\begin{equation}
\label{eq:softmax}
s_j = \frac{e^{z_j}}{\sum_{k=1}^{C} e^{z_k}},
\end{equation}
donde $C$ es el número de clases y $z_j$ es el valor de la neurona $j$ antes de aplicar softmax.
\end{definicion}

\paragraph{Propiedades:}
\begin{itemize}
  \item Cada $s_j$ esté en el intervalo $(0, 1)$.
  \item La suma de todos los $s_j$ sea $1$: $\sum_{j=1}^{C} s_j = 1$.
\end{itemize}
Esto permite interpretar la salida como una distribución de probabilidad sobre las clases.

\emph{Ejemplo: Clasificación de estados de ánimo} (4 clases): pena, alegría, depresión, ansiedad. La salida de la red es un vector de cuatro neuronas. Si softmax asigna $0.90$ a la clase ``alegría'', significa que el modelo predice con un $90\%$ de probabilidad que el estado de ánimo es alegría.

\begin{advertencia}
Es importante no confundir la \textbf{función de activación} (usada en capas ocultas, generalmente ReLU) con la \textbf{función de salida} (usada en la capa de salida, como softmax o identidad). Incluso una función como sigmoide, que no se recomienda como activación en capas ocultas, puede utilizarse como función de salida para problemas de clasificación binaria.
\end{advertencia}

\subsection{Entrenamiento de la red neuronal}

\begin{definicion}
\textbf{Entrenamiento}: el proceso mediante el cual la red encuentra los valores óptimos para todos sus parámetros (pesos y sesgos) de modo que resuelva el problema de forma satisfactoria.
\end{definicion}

Para entrenar, se inicializan los pesos con valores aleatorios pequeños (centrados en cero) y se itera entre la primera capa oculta y la capa de salida (el \textbf{forward pass}), ajustando los parámetros mediante retropropagación.

\paragraph{Datos y etiquetas}

Para entrenar una red se necesitan:

\begin{itemize}[topsep=2pt,itemsep=1pt]
  \item $\vec{y}$ (sin sombrero): la \textbf{etiqueta} correcta para cada dato de entrada (el valor esperado).
  \item $\hat{\mathbf{y}}$ (con sombrero): la \textbf{predicción} que calcula la red durante el entrenamiento.
\end{itemize}

El objetivo es que $\hat{\mathbf{y}} \approx \vec{y}$.

\subsubsection{Función de error y pérdida (loss)}

Para cuantificar el error, se define una \textbf{función de pérdida} que mide la discrepancia entre la salida esperada $\vec{y}$ y la predicción $\hat{\mathbf{y}}$.

\begin{definicion}
\textbf{Función de pérdida} (\emph{loss function}): función que mide la diferencia entre la salida esperada $\vec{y}$ y la salida calculada $\hat{\mathbf{y}}$, devolviendo un escalar que indica qué tan mal se está desempeñando el modelo en un ejemplo o en un lote.
\end{definicion}

Para un conjunto de $n$ ejemplos, la pérdida promedio es:

\begin{equation}
\label{eq:loss}
\mathcal{L} = \frac{1}{n} \sum_{i=1}^{n} \ell\!\left( \hat{\mathbf{y}}^{(i)},\, \vec{y}^{(i)} \right),
\end{equation}

donde $\ell$ es la función de error para un solo ejemplo (por ejemplo, error cuadrático medio o entropía cruzada).

\paragraph{Dependencia de los parámetros}

La función de pérdida depende de \emph{todos} los parámetros de la red:

\begin{equation}
\label{eq:theta}
\Theta = \left\{ \vec{W}^{(i)}, \vec{b}^{(i)}, \vec{U}, \vec{c} \right\}_{i=1}^{L}
\end{equation}

El objetivo del entrenamiento es encontrar el conjunto de parámetros $\Theta$ que \textbf{minimiza} la función de pérdida:

Este problema de minimización se resuelve mediante el algoritmo de \textbf{descenso del gradiente} y su variante \emph{backpropagation}, que calcula los gradientes de la pérdida con respecto a cada parámetro.

\paragraph{Interpretación de la pérdida y exactitud}

\begin{itemize}[topsep=2pt,itemsep=1pt]
  \item La \textbf{pérdida} debe ser \emph{cercana a 0}, pero no exactamente 0.
  \item Si la pérdida es exactamente 0, el modelo probablemente \emph{memorizó} el conjunto de entrenamiento y no generalizará bien a datos nuevos (\textbf{sobreajuste}).
  \item La \textbf{exactitud} (\emph{accuracy}) es el porcentaje de predicciones correctas sobre el total de ejemplos.
  \item Idealmente, la exactitud debe ser alta (por ejemplo, superior a 95\%), pero inferior a 100\% para asegurar generalización.
\end{itemize}